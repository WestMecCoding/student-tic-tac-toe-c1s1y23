# This is the project to submit your tic-tac-toe code

Paste your final working console code into the the myGame.js file

run 
> `git add .`
> `git commit -m "Final game code`
> `git push -u remote origin`